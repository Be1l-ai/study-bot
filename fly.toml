app = "study-bot"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  DB_PATH = "/data/study_bot.db"

[processes]
  worker = "python bot.py"

[[mounts]]
  source = "study_bot_data"
  destination = "/data"

[vm]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1